export default function updateBranchCategories(branchObj, treesToUpdate, isCorrect) {
    const newBranchObj = {
        unselected: [],
        selected: [],
        finished: [],
    }

    console.log(branchObj.unselected[0])
    console.log(treesToUpdate[0])

    
}